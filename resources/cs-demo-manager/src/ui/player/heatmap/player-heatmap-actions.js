import { createAction } from '@reduxjs/toolkit';
export const radiusChanged = createAction('player/heatmap/radiusChanged');
export const blurChanged = createAction('player/heatmap/blurChanged');
export const opacityChanged = createAction('player/heatmap/opacityChanged');
export const fetchPointsSuccess = createAction('player/heatmap/fetchPointsSuccess');
//# sourceMappingURL=player-heatmap-actions.js.map