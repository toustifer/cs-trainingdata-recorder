import { createAction } from '@reduxjs/toolkit';
export const fetchPointsSuccess = createAction('match/heatmap/fetchPointsSuccess');
export const radiusChanged = createAction('match/heatmap/radiusChanged');
export const blurChanged = createAction('match/heatmap/blurChanged');
export const opacityChanged = createAction('match/heatmap/opacityChanged');
//# sourceMappingURL=match-heatmap-actions.js.map