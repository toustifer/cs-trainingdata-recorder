import { createAction } from '@reduxjs/toolkit';
export const radiusChanged = createAction('team/heatmap/radiusChanged');
export const blurChanged = createAction('team/heatmap/blurChanged');
export const opacityChanged = createAction('team/heatmap/opacityChanged');
export const fetchPointsSuccess = createAction('team/heatmap/fetchPointsSuccess');
//# sourceMappingURL=team-heatmap-actions.js.map